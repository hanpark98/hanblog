---
const { src, alt = '', poster, class: klass = '', ...rest } = Astro.props
// src 예: "/videos/sample.mp4"
const posterAuto = poster ?? src.replace(/\.mp4$/i, '.poster.jpg') // 자동 매칭
---
<video
  src={src}
  poster={posterAuto}
  controls
  class={klass}
  {...rest}
/>
