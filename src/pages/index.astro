---
import Layout from '../layouts/Layout.astro';
---

<Layout title="Welcome">
  <section 
    id="hero-bg"
    class="w-full h-screen flex items-center justify-center cursor-pointer transition-colors duration-500"
    style="background: linear-gradient(120deg, #4f8cff, #f8cdda 80%);"
    onclick="window.location.href='/posts'"
  >
    <div id="hero-text" class="text-center px-4 max-w-[90vw]">
      <h1 class="text-3xl md:text-5xl lg:text-6xl font-extrabold mb-4 text-white leading-snug drop-shadow">
        한결
      </h1>
      <p class="text-base md:text-lg text-white drop-shadow">
        나임에 모자람 없어라
      </p>
    </div>
    <script>
      const hero = document.getElementById('hero-bg');
      // 고정색상 (시작, 끝)
      const colorStart = "#4f8cff";
      const colorEnd = "#f8cdda";
      window.addEventListener('mousemove', function(e) {
        // 마우스 X좌표에 따라 중간색 HSL값 변화
        const x = e.clientX / window.innerWidth;
        const hue = Math.round(200 + x * 100); // 200~300 범위의 블루~핑크(원하는 범위로 조정 가능)
        const midColor = `hsl(${hue}, 70%, 70%)`;
        // 3색 그라디언트
        hero.style.background = `linear-gradient(120deg, ${colorStart}, ${midColor} 50%, ${colorEnd})`;
      });
    </script>
  </section>
</Layout>